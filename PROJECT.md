# Lyra / Syno 프로젝트 개요

> 이 문서는 프로젝트의 정체성, 현재 구현 범위, 데이터 운영 원칙과 다음 작업의 기준을 한곳에 정리한 문서다.
> 현황 숫자는 `node scripts/project-status.mjs --write`가 갱신한다 (아래 표).

## 한 문장 정의

**Lyra / Syno는 좋아한 음악의 가사와 번역, 감상한 영화의 평점과 기억을 서로 연결해 축적하고, 시간이 흐르며 드러나는 한 사람의 문화적 취향을 다시 읽어내는 개인 아카이브다.**

음악 영역은 **Lyra**, 영화 영역은 **Syno**라는 이름을 사용한다. 둘은 별개의 서비스가 아니라 날짜, 시대, 국가, 장르, 감정과 키워드를 통해 이어지는 하나의 기록 체계다.

## 프로젝트의 정체성

이 프로젝트는 음원 스트리밍 서비스나 범용 영화 데이터베이스를 만드는 일이 아니다. 핵심은 다음 네 가지다.

1. **원문과 개인 해석을 함께 보존한다.** 음악은 원문 가사, 한글 번역, 일본어 독음, 연별 해설과 개인 코멘트를 한 기록에 담는다.
2. **좋아한 것과 본 것을 구분한다.** 직접 큐레이션한 음악·영화 기록과 왓챠에서 가져온 전체 관람 이력을 별도 데이터로 유지한다.
3. **통계보다 맥락을 우선한다.** 단순한 개수 집계에서 끝내지 않고 감정, 가사 키워드, 시대와 인물의 반복을 통해 취향의 형태를 보여준다.
4. **AI는 저자가 아니라 보조 작업자다.** AI가 번역·분류·초안을 만들 수는 있지만 원본, 식별자, 최종 판단과 데이터 반영 권한은 코드와 사람에게 남긴다.

## 현재 데이터 현황

<!-- status:start -->
_2026-08-14 기준 · `node scripts/project-status.mjs --write`로 갱신_

| 항목 | 수치 |
|---|---|
| 곡 | 768 |
| 영화(.md) · 평가한 영화 | 50 · 1045 |
| 커버 | 765/768 (100%) |
| 미리듣기 | 629/768 (82%) |
| Apple 정확 링크 | 613/768 (80%) (나머지는 검색 링크) |
| 연도 | 767/768 (100%) |
| 코멘트 · 키워드 · 감정 | 768/768 (100%) · 768/768 (100%) · 768/768 (100%) |
| 보완 대기 곡 | 5 |
| 남은 번역 · 독음 | 1줄 · 2줄 |
| 데이터 린트 | 오류 0 · 경고 154 |
<!-- status:end -->

인스타 원본 연결 734건, 보류 항목 195건(가사 부재 등으로 자동 반영하지 않고 대기),
가사 모티프 8개, 음악 추천 100곡, 영화 추천 100편.

보완 대기 항목은 `data/needs-work.json`에서 추적한다. 서비스 장애가 아니라 데이터 품질
작업 대기열이다. 린트 경고의 상당수는 같은 원문 구절에 서로 다른 번역이 붙은 경우인데,
반복 후렴을 문맥에 따라 달리 옮긴 정상 사례가 섞여 있어 기계적으로 통일하지 않고
가사 정확성 감사 화면에서 원전과 문맥을 확인한 뒤 판단한다.

## 현재 제공 기능

### Lyra: 음악 기록

- 앨범아트 중심 홈과 곡·가수·가사 통합 검색
- 국가, 연대, 가수, 감정과 태그별 탐색 및 공유 가능한 URL 상태
- 원문·한글 번역·일본어 독음의 대역 표시와 연 단위 해설
- 읽기 모드, 글자 크기, 진행률, 키보드 이전·다음 이동
- 30초 미리듣기와 페이지 전환 후에도 유지되는 전역 플레이어
- YouTube 연결, 관련 곡, 가사 키워드 기반 재탐색
- 선택한 구절을 1080×1350 이미지 카드로 만드는 공유 기능
- 장르·시대·권역·아티스트·감정·키워드 기반 음악 취향 분석
- 전체 가사를 가로지르는 모티프 클러스터와 취향 기반 추천 곡

### Syno: 영화 기록

- 제목, 감독, 배우, 줄거리 통합 검색
- 영화·드라마, 국가, 장르, 별점 필터와 다양한 정렬
- 큐레이션한 영화 상세 기록과 왓챠 별점 1,045편 탐색
- 별점 분포, 국가·장르·감독·배우·연대·상영시간별 취향 분석
- 감독·배우 역색인 인물 페이지와 참여 작품 연결
- 이미 본 작품을 제외한 취향 기반 영화 추천

### 두 영역을 잇는 기능

- 같은 날의 음악과 영화를 묶는 문화 아카이브
- 음악과 영화를 함께 보여주는 연도·태그 페이지
- 곡 상세의 `이 시대의 영화`, 영화 상세의 `이 시대의 음악`
- 월간·연간 기록량, 대표 감정, 키워드와 작품을 정리하는 문화 결산
- 날짜별 감정과 가사 키워드를 읽는 키워드 일기
- 음악, 가사, 영화, 평가 이력, 감독과 배우를 포괄하는 전역 검색
- 국가·연대·장르·태그·기록 시간과 감정 변화 시계열 통계

### 관리자 기능

- iTunes 미국·한국·일본 스토어 통합 검색과 곡 메타데이터 불러오기
- lrclib 가사 검색, Gemini 번역, 일본어 독음과 메타 초안 생성
- TMDB 검색을 통한 영화 등록과 왓챠 데이터 가져오기
- 곡·영화 추가, 수정, 삭제 및 온라인 GitHub 커밋 저장
- 가사 품질 재검사, 원전 대조 감사, 수정 근거와 이력 기록
- 누락 데이터 진단, 대량 작업 꾸러미 다운로드, 결과 검증과 선택 반영
- 음악·영화 취향 리포트, 모티프, 추천 목록 생성

## 데이터와 시스템 구조

프로젝트는 Next.js 15 App Router와 React 19로 구성되어 Vercel에 배포된다. 별도 데이터베이스 없이 사람이 읽고 수정할 수 있는 파일을 원본 데이터로 사용한다.

```text
songs/*.md                 곡별 메타데이터, 원문 가사, 독음, 번역, 해설
movies/*.md                큐레이션 영화별 메타데이터, 줄거리, 감상
data/watcha-movies.json    전체 영화 별점 이력
data/*-report.json         취향 분석, 검증, 감사 결과
data/motifs.json           가사 모티프
data/*-recs.json           음악·영화 추천 누적 결과
lib/                       파서, 집계, 검증, 외부 API와 저장 계층
app/                       공개 화면, 관리자 화면과 API
scripts/                   임포트, 진단, 검증, 린트와 빌드 자동화
```

로컬에서는 파일시스템에 직접 저장한다. Vercel에서는 읽기 전용 파일시스템 대신 GitHub Contents API로 저장소에 커밋하고, 그 커밋이 재배포를 시작한다. 관리자 영역은 HMAC 서명 쿠키로 보호하며 비밀번호 원문을 쿠키에 저장하지 않는다.

## 원본과 품질 보존 원칙

- 인스타 캡션을 가져온 곡은 원본 해시를 보존하며 `source_hash`를 갱신하지 않는다.
- 원문 가사를 고치려면 공식 가사, 앨범 북클릿, 공식 영상 자막 등 복수 근거를 확인한다.
- 수정한 줄과 근거는 `data/lyrics-corrections.json`에 남긴다.
- 자동 감사의 결과는 오류 확정이 아니라 사람이 볼 우선순위다.
- 곡의 `slug`, 제목, 아티스트처럼 레코드를 식별하는 값은 대량 AI 결과로 변경하지 않는다.
- 감정, 장르, 국가는 닫힌 어휘로 검증하고 키워드는 개수와 중복, 가사 근거를 검사한다.
- 기존 값은 기본적으로 덮어쓰지 않으며 명시적인 덮어쓰기 작업만 허용한다.

품질 게이트는 `pnpm check` 하나로 테스트, 콘텐츠 린트, 프로덕션 빌드와 주요 경로 smoke test를 실행한다. 현재 `lib/*.test.mjs`의 테스트 19개와 공개·관리자 경로 smoke 검사 17개가 통과하며, GitHub Actions가 `main` push와 pull request마다 같은 검사를 수행한다.

## AI 작업 원칙

AI 작업은 호출량과 작업 성격에 따라 소량 작업과 대량 작업으로 분리한다.

| 구분 | Gemini | Claude·ChatGPT | 로컬 코드 |
|---|---|---|---|
| 역할 | 한 건의 번역·설명·메타 초안, 부족 항목의 우선순위 판단 | 전체 데이터 대상 생성·재분류·리포트·모티프 작업 | 진단, 배치 생성, 형식 검증, 병합, 이력 기록 |
| 입력 규모 | 새 음악·영화 한 건 또는 제목과 부족 항목만 담은 요약 | 곡 30개, 영화 20개 단위의 명시적 작업 꾸러미 | 전체 원본 데이터 |
| 허용 작업 | 가사 번역, 일본어 독음, 설명글, 단일 항목 태그 초안 | 전체 메타 재생성, 키워드·감정 일괄 추출, 취향 리포트, 모티프 | 불변 필드 보호와 최종 반영 |
| 결과 지위 | 검수 전 초안 | 검증 전 중간 산출물 | 검증을 통과한 데이터만 원본에 반영 |

표준 대량 작업 흐름은 다음과 같다.

```text
1. 진단    scripts/needs-work.mjs
           → data/needs-work.json

2. 선별    Gemini에는 제목·아티스트·부족 항목만 전달
           → 처리 우선순위 목록

3. 생성    Claude 또는 ChatGPT에 곡 30개 / 영화 20개씩 전달
           → 고정 JSON 스키마의 중간 결과

4. 검증    scripts/validate-bulk.mjs
           → data/validation-report.json

5. 반영    통과한 필드만 기존 레코드에 병합
           → pnpm check 후 배포
```

Gemini의 무료 호출은 새 항목을 한 건씩 등록할 때 가장 가치가 높은 번역과 설명에 사용한다. 전체 768곡을 순회하는 작업은 Gemini 관리자 버튼으로 직접 실행하지 않고, 진단 결과를 내려받아 Claude나 ChatGPT에서 배치 처리한 뒤 코드 검증을 거치는 것을 기본 운영 정책으로 삼는다.

## 지금까지의 작업 흐름

1. Markdown 기반 정적 가사 블로그로 시작해 검색, 태그와 곡 상세를 구현했다.
2. 영어에서 일본어·한국어로 범위를 넓히고 독음·번역·연 해설 데이터 형식을 정립했다.
3. CLI 등록을 브라우저 관리자 화면으로 확장하고 iTunes, lrclib, Gemini를 연결했다.
4. 인증, GitHub 저장 백엔드와 Vercel 자동 배포를 도입해 온라인에서도 콘텐츠를 관리하게 했다.
5. 키워드, 감정, 일기, 통계와 음악 취향 분석을 추가해 목록을 해석 가능한 아카이브로 확장했다.
6. Syno 영화 영역, 왓챠 이력, TMDB 메타데이터, 인물·취향·추천 기능을 통합했다.
7. 음악과 영화를 날짜, 태그, 시대와 결산으로 교차 연결했다.
8. 인스타 원본 임포트, 무손실 검증, 가사 수정 이력과 자동 품질 감사를 구축했다.
9. 대량 AI 작업의 비용과 위험을 줄이기 위해 진단→선별→생성→검증→반영 파이프라인을 만들었다.
10. 테스트·데이터 린트·빌드·smoke를 하나의 CI 품질 게이트로 통합했다.

여러 시각적 재설계와 감정 모델, 인공적인 발견 경로도 실험했지만 프로젝트의 핵심 경험을 흐린다고 판단해 제거했다. 현재 방향은 화려한 탐색 장치보다 **기록 자체, 읽기 경험, 실제 취향에서 나온 연결**을 우선한다.

## 현재 한계와 다음 우선순위

1. `data/needs-work.json`의 음악 30곡과 영화 16편을 근거 자료와 함께 보완한다.
2. 인스타 보류 195건은 가사 존재 여부, 중복, 음악 외 게시물로 분류하고 자동 등록하지 않는다.
3. 가사 모티프에 빈 대표 구절이 포함된 항목을 재검증하고 실제 가사와 연결된 인용만 유지한다.
4. AI 취향 리포트와 추천은 사실처럼 단정하지 않고 집계 수치와 생성 시점을 함께 표시한다.
5. 대량 작업은 배치별 검증 보고서와 변경 이력을 남기고 한 번에 전체 데이터를 덮어쓰지 않는다.
6. 데이터 규모가 파일 기반 빌드와 GitHub Contents API의 실용 한계를 넘을 때만 데이터베이스 이전을 검토한다.

## 문서 안내

- [README.md](README.md): 설치, 실행, 콘텐츠 추가와 배포에 필요한 빠른 안내
- [DEVELOPMENT.md](DEVELOPMENT.md): 데이터 모델, 세부 기능, 아키텍처와 개발 이력
- [scripts/watcha-bookmarklet.md](scripts/watcha-bookmarklet.md): 왓챠피디아 별점 내보내기 사용법
- `data/needs-work.json`: 현재 데이터 보완 대기열
- `data/validation-report.json`: 최근 대량 작업 검증·반영 결과
