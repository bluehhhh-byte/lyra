# Slug 무결성 감사

2026-08-27에 `node scripts/audit-slugs.mjs`를 실행했다.

- 곡/영화 slug 교차 충돌: **0건**
- 현재 신규 등록 규칙으로 다시 만들면 달라지는 기존 URL: **47건**

47건은 예전 영문·한글 표기, 제목 변경, 충돌 접미사(`-2`)가 남은 역사적 URL이다. 감사 스크립트가 현재 slug와 예상 slug를 전부 출력하지만 URL을 바꾸지는 않는다. 교차 충돌만 감사 실패로 처리하고, 재생성 불일치는 링크 보존을 위해 보고로 끝낸다.
