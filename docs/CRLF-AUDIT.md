# CRLF 정규화 감사

곡·영화의 파일 백업, DB 목록, DB 메타 목록, DB 상세 단건 경로를 확인했다. 기존에는 각 호출자가 CRLF를 LF로 바꿔 파서에 넘겼기 때문에 새 호출 경로가 변환을 빼먹을 수 있었다.

정규화를 `parseFrontmatter`와 `parseLyrics` 입구로 옮겼다. 이제 파일·DB 여부와 호출자에 관계없이 파서가 `CRLF`, 단독 `CR`, `LF`를 먼저 `LF`로 통일한다. 직접 파서 호출까지 포함한 정규화 누락 경로는 **0건**이다.
